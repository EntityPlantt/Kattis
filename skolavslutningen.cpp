#include <iostream>
#include <string.h>
using namespace std;
int main() {
    int m, n, k, newColor = 0;
    cin >> n >> m >> k;
    char classColors[k], columnColors[m];
    memset(classColors, -1, sizeof(classColors));
    memset(columnColors, -1, sizeof(columnColors));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            cin >> x;
            if (); //
        }
    }
    return 0;
}